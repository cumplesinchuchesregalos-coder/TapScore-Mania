/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // Adds the 'export' output option

  // Any existing configuration options would go here
  // For example:
  // reactStrictMode: true,
  // swcMinify: true,
  // images: {
  //   unoptimized: true,
  // },
};

module.exports = nextConfig;